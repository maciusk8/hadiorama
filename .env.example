# Home Assistant URL (server-side only, NOT exposed to frontend)
HA_URL=http://192.168.1.XX:8123

# Client ID = public URL of this app as seen by the browser
# Must match the host/port users use to access the dashboard
CLIENT_ID=http://192.168.1.XX:3000
